"use client";
import { useAbout } from "../hooks/useAbout";

export default function About() {
  return (
    <section
      id="About"
      className="h-full flex flex-col gap-6 bg-white"
    ></section>
  );
}
